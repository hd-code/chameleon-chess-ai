import { IPosition } from '../types';
/** Type guard for `IPosition`. */
export declare function isPosition(position: any): position is IPosition;
export declare function isSamePosition(a: IPosition, b: IPosition): boolean;
export declare function isInPositions(postion: IPosition, positions: IPosition[]): boolean;
export declare function sortPositions(a: IPosition, b: IPosition): number;
