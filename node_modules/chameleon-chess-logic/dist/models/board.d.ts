import { TBoard, EFieldColor, IPosition } from '../types';
/** Type guard for `EFieldColor`. */
export declare function isFieldColor(fieldColor: any): fieldColor is EFieldColor;
export declare function getBoard(): TBoard;
export declare function getBoardSize(): {
    numOfRows: number;
    numOfCols: number;
};
/** Returns the color of the field on the game board at a given position. */
export declare function getFieldColor(position: IPosition): EFieldColor;
