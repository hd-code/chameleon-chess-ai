import { IPawn, ILimits, EPlayer, IPosition } from '../types';
/** Type guard for `IPawn`. */
export declare function isPawn(pawn: any): pawn is IPawn;
export declare function getStartPawns(player: EPlayer): IPawn[];
export declare function getPawnsIAtPosition(pawns: IPawn[], position: IPosition): number;
export declare function getMoves(pawnIndex: number, pawns: IPawn[], limits: ILimits): IPosition[];
/** Checks if there is a pawn in deadlock and returns its index in the pawns
 * array. This occurs when the board has shrunken to the smallest possible size
 * (3x3). If a pawn happens to be a knight on the center field, it can no longer
 * do any move. Therefore, it is in deadlock. */
export declare function getIndexOfPawnInDeadlock(pawns: IPawn[], limits: ILimits): number;
