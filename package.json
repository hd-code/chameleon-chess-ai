{
  "name": "chameleon-chess-ai",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "flush-cache": "rm -rf data; mkdir data",
    "start": "ts-node scripts/*.ts",
    "test": "mocha -r ts-node/register 'test/**/*.ts'"
  },
  "devDependencies": {
    "@types/node": "^12.12.36",
    "ts-node": "^8.8.1",
    "typescript": "^3.8.3"
  }
}